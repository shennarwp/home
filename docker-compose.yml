version: "3"
services:
  home:
    image: nginx:alpine
    container_name: home
    restart: always
    expose:
      - 80
    volumes:
      - ./:/usr/share/nginx/html/
    networks:
      - nginx

networks:
  nginx:
    external: true
